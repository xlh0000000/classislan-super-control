# 控制平面的数据目录（容器内固定为 /data）
CLASSISLAND_CONTROL_DATA_DIR=./data
NITRO_HOST=0.0.0.0
NITRO_PORT=3000
# 首设引导令牌：生产环境必填，至少 16 位随机字符串（建议 `openssl rand -base64 32`）
# 初始化完成后仍应保留，避免误删后重启被拒。
CLASSISLAND_CONTROL_BOOTSTRAP_TOKEN=